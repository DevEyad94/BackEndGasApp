namespace BackEndGasApp.Constants
{
    public static class Constant
    {
        private static string _environment;

        public static void SetEnvironment(string environment)
        {
            _environment = environment;
        }
    }
}
